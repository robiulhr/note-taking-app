export interface noteType {
  id: string;
  noteTitle: string;
  noteDescription: string;
  noteTags: string[];
}

export interface tagType {
  name: string;
  color: string;
  description: string;
}
